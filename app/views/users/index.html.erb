<h1>All Users</h1>

<% @users.each do |user| %>
   <p>
      Name: <%= user.name %>, 
      Email: <%= user.email %>,
      Phone_number: <%= user.phone_number %>
   </p>
<% end %>

<%#= link_to "Back to Home", root_path, class: "dropdown-item" %>
